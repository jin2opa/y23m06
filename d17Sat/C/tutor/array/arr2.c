#include <stdio.h>

int main(int argc, char const *argv[])
{
  /* char *(*name) = {"jin", "min", "hin"};
  for(int i=0; i<sizeof(name)/sizeof(char*); i++){
    printf("%s\n", name[i]);
  } */
  return 0;
}
